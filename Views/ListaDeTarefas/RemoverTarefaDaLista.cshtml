@model td4.Models.ListaDeTarefasModel

@{
    ViewData["Title"] = "Confirmar Remoção";
}

<h1>Confirmar Remoção</h1>

<div>
    <h4>Você tem certeza que deseja remover a tarefa da lista?</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Tarefa
        </dt>
        <dd class="col-sm-10">
            @Model.Tarefa
        </dd>
        <dt class="col-sm-2">
            Data
        </dt>
        <dd class="col-sm-10">
           @Model.Data.Day/@Model.Data.Month
        </dd>
        <dt class="col-sm-2">
            Hora
        </dt>
        <dd class="col-sm-10">
           @Model.Hora.ToString(@"hh\:mm")
        </dd>
    </dl>
    
    <form asp-action="RemoverTarefa">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <input type="submit" value="Remover" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>
